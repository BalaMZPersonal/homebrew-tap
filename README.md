# homebrew-tap

Homebrew tap for GTMShip.

Install with:

```bash
brew install BalaMZPersonal/tap/gtmship
```

Start on a desktop machine with:

```bash
gtmship open
```

Start on a headless Linux VM with:

```bash
gtmship start
gtmship status
```
